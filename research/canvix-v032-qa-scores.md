# Canvix v0.3.2 — VLM visual QA scores

| 01-editor | 8.5 | Topbar gradient is too linear and lacks the specific Canva 2025 purple-to-cyan depth; Icon rail is ~72px but icons are slightly undersized and misaligned; Typography uses a generic sans-serif instead of the distinct Canva Sans font. |
| 03-fill-menu | 8.5 | Topbar height is ~44px instead of the standard ~56px; Left sidebar icon rail is ~60px instead of 72px; Fill dialog lacks the characteristic 28-32px border radius and subtle backdrop blur of modern Canva. |
| 04-gradient-applied | 8.5 | Topbar gradient is too blue; left rail icons are too small (~48px vs 72px); dialog border radius is slightly too sharp. |
| 06-font-search-cinzel | 8.5 | Top bar gradient is cyan-to-purple instead of purple-to-dark; left rail icons are ~48px instead of the standard 72px; font picker dropdown lacks the specific rounded container style and spacing of Canva's 2025 UI. |
| 08-photos-panel | 8.4 | Left sidebar is ~280px wide instead of the standard 72px icon rail; topbar height and font weight slightly off; placeholder images lack realistic stock photo metadata/overlays. |
| 10-photo-added | 8.5 | Top bar gradient is too linear and lacks the subtle cyan-to-purple transition of the real editor. Left sidebar icons are slightly too small (real Canva uses ~72px rail). Typography in the sidebar lacks the specific weight and tracking of Canva Sans. |
| 11-export-pdf | 8.5 | Topbar gradient is too linear and lacks the deep purple-to-violet transition of the real editor. Dialog border radius is slightly smaller than the standard 28-32px. Typography lacks the specific weight and spacing of Canva Sans. |
| 12-apps-grid | 8.5 | Topbar height is ~48px instead of the standard ~56px; Left rail width is slightly narrower than the canonical 72px; Typography lacks the specific geometric weight of Canva Sans. |
| 13-magic-write | 9.2 | Top bar height is ~44px instead of the standard ~56px; left rail icons are ~60px instead of the standard ~72px; 'Generate' button gradient is slightly too blue compared to Canva's deep purple. |
| 16-ai-image-result | 8.5 | Topbar gradient is too linear and lacks the specific cyan-to-purple transition of the real editor. Left sidebar icon rail is ~60px instead of the standard 72px. Typography and button border radii are slightly smaller than the 28-32px standard. |
| 17-templates-granny | 8.5 | Topbar gradient is slightly too blue; sidebar icon rail is narrower than the standard 72px; selection handles are circles instead of squares. |

**Average score: 8.6 / 10 across 11 screenshots**
